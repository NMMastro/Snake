const canvas = document.getElementById("gameCanvas");
const ctx = canvas.getContext("2d");

// Fill the canvas with a dark green background
ctx.fillStyle = "#2d4a1e";
ctx.fillRect(0, 0, canvas.width, canvas.height);
